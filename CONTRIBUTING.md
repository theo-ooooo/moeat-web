# 모잇 웹 기여 가이드

## 브랜치 전략

모잇은 `GitHub Flow`를 사용한다. `main`은 항상 배포 가능한 상태로 유지하며 장기 `develop` 브랜치를 두지 않는다.

1. 작업 전에 GitHub 이슈를 만든다.
2. 최신 `main`에서 이슈 전용 브랜치를 만든다.
3. 하나의 브랜치는 하나의 이슈만 해결한다.
4. 첫 커밋을 푸시한 뒤 Draft PR을 열어 진행 상황을 공유한다.
5. 검증이 끝나면 리뷰 가능한 상태로 전환한다.
6. 승인과 필수 검사를 통과한 뒤 Squash merge한다.
7. 병합된 브랜치는 삭제한다.

## 브랜치 이름

```text
feat/{이슈번호}-{짧은-설명}
fix/{이슈번호}-{짧은-설명}
docs/{이슈번호}-{짧은-설명}
chore/{이슈번호}-{짧은-설명}
refactor/{이슈번호}-{짧은-설명}
test/{이슈번호}-{짧은-설명}
hotfix/{이슈번호}-{짧은-설명}
```

예시: `feat/12-solo-recommendation`

브랜치 설명은 영문 kebab-case를 사용한다. `hotfix`는 운영 장애나 중대한 보안 문제처럼 즉시 수정해야 하는 경우에만 사용한다.

## 커밋

커밋 제목은 다음 접두사를 사용한다.

```text
feat: 기능 추가
fix: 버그 수정
docs: 문서 변경
chore: 설정 및 도구 변경
refactor: 동작을 바꾸지 않는 구조 개선
test: 테스트 추가 및 변경
style: 코드 동작과 무관한 형식 변경
```

커밋에는 가능한 한 하나의 논리적인 변경만 담는다.

## Pull Request

- 제목은 변경 결과가 드러나는 한글 문장으로 작성한다.
- 본문에 변경 이유, 주요 변경 내용, 검증 결과를 기록한다.
- `Closes #이슈번호`를 넣어 병합 시 이슈가 닫히도록 한다.
- UI 변경에는 모바일 화면 캡처를 첨부한다.
- API 변경이 필요하면 관련 서버 이슈 또는 PR을 연결한다.
- 리뷰 가능한 크기를 유지하고, 너무 커지면 이슈를 나눈다.

## 웹·서버 연동 작업

두 저장소에 걸친 기능은 이슈 제목에 같은 작업 키를 사용한다.

```text
[MEAL-001] 혼밥 추천 API 연동
```

각 PR 본문에서 상대 저장소의 이슈 또는 PR을 연결한다. 서버 API가 준비되지 않았다면 합의된 OpenAPI 계약을 기준으로 작업하며 임시 응답 구조를 사실상 표준으로 만들지 않는다.

